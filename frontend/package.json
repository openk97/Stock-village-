{
  "name": "stock-village-frontend",
  "version": "1.0.0",
  "description": "Stock Village frontend (Vite build: bundling + minify + cache-busted assets)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
